# Exemplo de SECRET_KEY (Gere uma chave segura para produção!)
SECRET_KEY="c_sua_chave_secreta_aqui_123456"

# Configurações de Banco de Dados
# Se usar um banco de dados externo como PostgreSQL ou MySQL, a sintaxe pode ser diferente (ex: DATABASE_URL)
# Se estiver usando o SQLite padrão, essas variáveis podem não ser necessárias.
# DATABASE_URL="sqlite:///db.sqlite3" # Exemplo para SQLite

# Configurações de E-mail (que causou o erro)
# Substitua com suas credenciais, se necessário, ou deixe em branco se não for testar o envio de e-mail imediatamente.
EMAIL_HOST_USER="seu_email_aqui@exemplo.com"
EMAIL_HOST_PASSWORD="sua_senha_ou_app_password"
EMAIL_HOST="smtp.exemplo.com"
EMAIL_PORT=587
EMAIL_USE_TLS=True

# Outras variáveis comuns (como para o modo de debug)
DEBUG=True